const p = require("../../package.json")

export const APP_NAME = "SidePanel for Backlog"

// アプリケーションのバージョン番号
export const APP_VERSION = p.version
export const APP_VERSION_WITH_PREFIX = `v${APP_VERSION}`

export const OPTIONS_PAGE_URL = "options.html"
